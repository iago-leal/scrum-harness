# scrum-harness

Plugins de **SCRUM** para o [DeepSeek Harness](../deepseek-harness/): o processo ágil executável pelo agente (tools), por humanos (`/scrum` e um quadro visual na GUI web) e persistido fora das conversas.

Hierarquia semântica do backlog:

```
Release
  └─ Função (feature)
       └─ Backlog de produto (componente)
            └─ Tarefas
```

**Sprints** são janelas de tempo que selecionam Tarefas dessa hierarquia (Sprint Backlog), com quadro Kanban (`todo / in_progress / review / done`) e **cerimônias** registradas (Planning, Daily Standup, Review, Retrospectiva). Cada sprint pode ser **vinculada a uma Release** (no planejamento ou depois); o vínculo aparece em todas as vistas — chips de sprint na linha da release (árvore), seletor/badge na aba Sprints, cabeçalho do Board e saídas do `/scrum` e das tools.

**Ciclo de vida** (v0.2): todo item da hierarquia está em exatamente um de três estados — **vivo** (nas vistas principais), **arquivado** (`archivedAt`: concluído e guardado no **Arquivo**, reversível) ou **na lixeira** (`deletedAt`: soft delete na **Lixeira**, restaurável). Excluir sempre passa pela lixeira; a remoção física só acontece com *purge* / esvaziar lixeira, a partir da própria lixeira. Cascatas carimbam o mesmo timestamp na subárvore, então restaurar/desarquivar revive exatamente o que saiu junto (e reativa ancestrais quando preciso). Tarefas na sprint ativa não podem ser apagadas nem arquivadas; tarefas `done` arquivadas continuam contando nos totais das sprints históricas.

**Backlog na GUI** (v0.3, à la Azure DevOps): a aba Backlog é uma grade hierárquica com colunas alinhadas (**Item | Estado | Pontos | Sprint**) — ícone colorido por tipo (R/F/C/T), estado como ● + rótulo, rollup de progresso (barra + pontos concluídos/total) nos níveis pai, chevrons de expandir/recolher (com «Expandir/Recolher tudo»), menu **⋯** por linha e linhas-fantasma «＋ Novo item» por nível (Enter cria e mantém o foco). Os mesmos ícones/dots aparecem em Sprints, Arquivo e Lixeira.

**Work item form & estados multinível** (v0.4, à la Azure DevOps): clicar no título de qualquer linha do Backlog ou card do Board abre o **work item form** — modal com o conteúdo completo e editável (título, breadcrumb, estado, descrição, estimativa, data alvo, sprint; Esc fecha). Cada nível tem **workflow próprio**: componente `proposed → in_progress → done`, função `proposed → committed → in_progress → done` (campo com default no parse: mídia v0.3 carrega sem bump de versão). E a aba Board ganhou o pivô **Tarefas | Componentes | Funções**: além do Kanban da sprint, boards por nível cujas colunas são os estados do nível — arrastar o card muda o status, com rollup de progresso nos cards de pai.

**Um quadro por workspace** (v0.5): o `ctx.scrum` é um **gerente de quadros** — cada workspace tem seu próprio quadro em seu próprio meio de storage (`scrum_ws_<hash>.json`, chave derivada do caminho canônico via `boardNameOf`), aberto sob demanda e cacheado. As **tools** roteiam pelo cwd da sessão que chamou (`exec.agent.session.header.cwd`), o **`/scrum`** idem, a **API HTTP** recebe o workspace do cliente (`?workspace=` no GET, campo `workspace` no POST) e o **painel** segue o workspace da sessão corrente (chip 📁 no cabeçalho; fallback: workspace recente). Sessões sem cwd compartilham o quadro global (`scrum_global`). O quadro único anterior migra com `node scripts/migrate-v0.5.mjs` (adota o `scrum.json` como quadro deste repositório e guarda backup).

**Números de sprint & fluxo no board** (v0.6): a aba Sprints ganha **velocity** (barras SVG de pontos concluídos por sprint encerrada + média das últimas 3) e **burndown** por sprint (botão 📉 no card: linha real de pontos restantes × ideal, alimentada pelo carimbo `doneAt` que o `moveTask` grava ao entrar em `done` — campo opcional, mídia antiga carrega sem migração; a API expõe `stats` por sprint contando também tarefas arquivadas). O board de tarefas ganha **swimlanes por componente** (botão ☰ Raias, rollup por raia) e **WIP limits por coluna** (`wipLimits` opcional na sprint, editável clicando no contador da coluna — `n/limite` fica vermelho ao estourar; aviso visual, sem bloqueio, fiel ao Azure DevOps). Estados de pai seguem **manuais** por decisão de produto (estilo Azure): componente/função só mudam de coluna por arrasto, form ou tool. Gráficos em SVG puro, sem bibliotecas.

**Visual Primer** (v0.8): o painel adota o design system do GitHub via **@primer/primitives** — a camada de *design tokens* escolhida no spike da sprint (contra `@primer/css`, reescrita BEM, e `@primer/react`, bundle desproporcional). O `build.mjs` embarca as folhas de tokens como texto (`loader: { '.css': 'text' }`), `primer.ts` re-escopa os tokens de `:root` para os wrappers do painel (nada vaza para o shell) e o overlay carrega `data-color-mode`/`data-light-theme` — o tema fica escopado e o dark mode a um atributo de distância. O `styles.ts` fala só tokens semânticos com fallback literal (`var(--fgColor-accent, …)`); em cima disso: botões com tokens de componente (primário verde GitHub), inputs estilo form control, ícones/dots/chips como badges e issue labels, progresso verde de milestone, counters em pílula, work item form com anatomia de dialog Primer, backlog com cabeçalho de issue tracker, colunas/cards à la GitHub Projects (dot de estado no título da coluna) e gráficos SVG tokenizados.

**Agente ciente do quadro** (v0.7): o plugin `context-scrum` (padrão do `time-context` do DSH) intercepta o waterfall `agent/pre-step` e, no **1º step de cada turno**, resolve o quadro do workspace da sessão (`agent.session.header.cwd`) e injeta uma mensagem de contexto durável e atribuída ao plugin com o snapshot da **sprint ativa** — colunas com ids/pontos, estados explícitos dos pais e a instrução da disciplina de "board ao vivo" (mover a tarefa ao começar/terminar, manter os pais, registrar cerimônias). Só injeta **quando o snapshot mudou** desde a última injeção (o próprio texto é a chave, guardado em `WeakMap` por agente); sem sprint ativa, não injeta nada. Assim a disciplina que era hábito do agente vira contrato do harness — inclusive para o próprio agente que desenvolve este repositório.

**SCRUM como aba** (v0.9): o quadro deixa de ser botão na sidebar + overlay e vira **view de conversa de primeira classe** — uma entry no anel `conversation.view` (`{ id: 'scrum', order: 20 }`; Chat=0, Trajectory=10), a aba **▦ SCRUM** ao lado de Chat e Trajectory. O corpo inteiro do painel (Backlog/Board/Sprints/Arquivo/Lixeira + work item form) renderiza inline preenchendo a `viewArea` do shell, sem backdrop nem botão fechar: o anel monta só a view ativa, então o polling vira "enquanto montado" e o estado `open` morreu com o overlay. O workspace agora resolve pela **sessão da própria aba** (`useSessions(s => s.byId[sessionId].cwd)`, kit padrão que o slot entrega junto com `useWorkspaces`) — mais preciso que o "workspace corrente" do overlay. A view usa um **store dedicado por sessão**: a regra *one handle, one scope* do slot system (postmortem de 01/09 abaixo) proíbe reusar o handle root do antigo botão/painel — entre escopos, compartilha-se por dados (`/scrum-api`), nunca por handle. `Panel.tsx` e `ScrumButton.tsx` foram apagados; a dependência nova é só de tipos (`@deepseek-ai/dsh-client-ui-conversation`, zero bytes no bundle).

**Dark theme** (v0.10, action-item da retro da v0.8): toggle **🌙/☀️** no cabeçalho do painel. O `primer.ts` embarca **as duas folhas de tema** (`themes/light.css` + `themes/dark.css`, ambas atrás de `[data-color-mode]`/`[data-*-theme]` — bundle 236→360KB) e o root da view mantém `data-light-theme`/`data-dark-theme` fixos, alternando só `data-color-mode`: é ele que escolhe qual folha acende, e os tokens herdam até o work item form por descendência DOM. A escolha persiste em `localStorage` (`scrum-theme`, lida no init do store com guarda). A varredura de cores fixas confirmou o dividendo da tokenização da v0.8: **um** único conserto (hover do pivot, branco fixo → `--button-invisible-bgColor-hover`); os brancos translúcidos do cabeçalho ficam por design (vivem sobre `--bgColor-emphasis`, escuro nos dois temas), e gráficos SVG, chips e dots já falavam `var(--…)` — flipam de graça.

**Sprint multi-release** (v0.11): o vínculo sprint→release deixa de ser campo único — uma sprint pode avançar **várias releases** (a dor apareceu na spr-9, que era dupla e teve que nascer "sem release"). No domínio, `releaseIds: string[]` é a forma canônica com **migração no parse**: o `sprintSchema` ganhou um `.transform` que incorpora o `releaseId` legado (`ZodEffects` passa direto no `domainTable`, sem bump de versão de mídia), e `planSprint`/`updateItem` aceitam **os dois campos** — `releaseIds` substitui o conjunto inteiro (validado vivo, deduplicado; `[]` desvincula tudo) e `releaseId` segue como atalho retrocompatível (`''` desvincula). O purge de release remove **só o id purgado** do conjunto de cada sprint. Tools (`scrum_sprint_plan`/`scrum_item_update`) e API HTTP expõem `releaseIds`; o `/scrum` herda dos formatters (` → rel-1 nome, rel-2 nome`). Na GUI, o dropdown único da aba Sprints virou **chips** (um por release vinculada, × remove) + seletor "＋ vincular release…" com as não vinculadas; árvore e cabeçalho do Board mostram todos os vínculos. Implementação paralelizada em **3 subagents** com conjuntos de arquivos disjuntos (tools+API, GUI, e o spike da release seguinte), contrato de tipos congelado pelo domínio antes do fan-out.

**Motor da espiral** (v0.12, primeiro componente da rel-17 "Disciplinar pipeline com modelo espiral"): cada **componente** passa a caminhar por cinco fases — `requirements → design → tdd → construction → validation` — com **gates** para avançar (um passo por vez, nunca pula): requisitos **e** revisão adversarial preenchidos para sair de `requirements`; desenho preenchido para sair de `design`; ao menos uma task para sair de `tdd`; todas as tasks `done` para sair de `construction` (o `done` do componente ganha gate próprio no comp-47). Recuar é livre — a espiral revisita — e todo movimento vai para um `phaseLog` append-only. Quatro artefatos de texto (`requirements`, `requirementsReview`, `design`, `validation`) em **markdown com frontmatter YAML** consultável por agentes (`parseFrontmatter`, mini-parser próprio sem dependência: escalares, listas inline, objetos de um nível, blocos `- item`). Regras só no domínio, avaliadas **dentro** do mutator do `table.update` — tool (`scrum_component_phase`), API (`componentPhase`) e GUI encontram a mesma regra sem janela de leitura/verificação/escrita. Migração no parse (`done` legado → `validation`, o resto → `requirements`, sem bump). O componente foi feito **pela própria espiral**: requisitos v1 → revisão adversarial por subagent (4 achados ALTA, dois seriam bugs: o denominador de tasks "vivas" × `archiveCompleted`, e o default de migração punindo componentes `done`) → requisitos v2 → desenho v2 → TDD estrito (19 + 3 testes escritos e vermelhos antes do código) → validação com o dogfood: o comp-42 avançou de fase pela tool que ele mesmo entregou.

**Revisão adversarial com contrato** (v0.13, comp-48): o gate `requirements → design` deixa de aceitar "qualquer texto" e passa a exigir um **contrato verificável** — sem julgar conteúdo. No Model, a família `ArtifactContract<M>` (`contracts.ts`: campo, schema zod do frontmatter, corpo normalizado, **digest** sha1 curto do corpo) com `RequirementsContract` (frontmatter `version` inteiro + **carimbo humano `status: approved`** + corpo) e `ReviewContract` (a revisão como *par*: frontmatter `reviewer`/`reviewed_version`/`reviewed_digest`/`verdict`/`round`/`findings`, veredito `approved` com `findings.high 0`, cobrindo **esta** versão **e este texto** — o digest fecha a brecha do "editei sem bumpar"). O gate reporta **todas** as condições violadas de uma vez, nomeadas com o caminho do campo; `scrum_item_update` avisa cedo ("review contract: …") sem bloquear rascunhos; o `formatTree` marca ` · review stale` quando uma revisão válida deixou de cobrir os requisitos. O briefing do revisor virou **produto**: `ScrumBoard.reviewBrief(id)` (Model) → `formatReviewBrief` (View) → tool `scrum_component_review_brief` (Controller) — blocos em ordem fixa (requisitos com versão e digest, revisão anterior rotulada, desenho, tasks), o bloco **House conventions** (MVC + OO, frontmatter, gates, TDD) e o formato de resposta com o frontmatter **pré-preenchido**. É por esse bloco que as convenções da casa viajam a qualquer workspace: disciplina em README não se propaga; em tool, sim. O próprio comp-48 passou pela espiral: requisitos v1 → revisão (5 ALTA: `version` manual e `verdict: resolved` deixavam o gate contornável pelo próprio agente) → v2 aprovada → desenho → TDD estrito (17 + 5 testes vermelhos antes do código).

**Gate hard de done** (v0.14, comp-47): a espiral ganha **saída** — até aqui `status: done` de componente passava livre por tool, API e drag do board, e os dois primeiros componentes da rel-17 fecharam "à mão". Agora `done` só entra pelo domínio, avaliado **dentro do mutator** de `updateItem` sobre o estado *next* (artefatos do mesmo patch já aplicados) e **só na transição** para `done`: (a) fase `validation`, (b) ao menos uma task não-lixeira e todas `done` (arquivadas contam; regressão de task *depois* do `done` não o desfaz), (c) artefato `validation` honrando o `ValidationContract` — terceiro membro da família de contratos: `validated_at` ISO-8601 (entre aspas, pelo parser), `suite { tests ≥ 1, passed, skipped, wall_seconds, budget_seconds }` com `passed + skipped = tests`, `typecheck: clean`, `wall ≤ budget_seconds ≤ teto do quadro` (`DEFAULT_SUITE_BUDGET_SECONDS = 15`, injetável no construtor para o comp-50). Erro `done-gate` com **todas** as razões; `done → done` é no-op; voltar de `done` é livre e voltar a `done` passa pelo gate de novo. **Uma fonte de verdade**: `doneReadiness(id)` é a mesma lógica do gate, alimenta o aviso da tool ("validation contract: …") e o `readyForDone` que o `tree()` anota **no Model** — a View só imprime ` · ready for done` (a rodada 1 da revisão pegou que a View decidiria diferente do gate: `tree()` omite tasks arquivadas, o gate as conta). A API já respondia 409 com `code` por construção; a GUI encontra o mesmo gate no drag. O comp-47 foi o **primeiro componente a passar pelo gate de requisitos com dentes** (comp-48): v1 → rodada 1 `needs-revision` → v2 com carimbo humano → rodada 2 `approved`, ambas pelo brief gerado pela tool — e o primeiro `done` pelo gate que ele mesmo entrega.

**Orçamento de tempo da suite** (v0.15, comp-50): o gate do comp-47 barrou o próprio comp-47 — a suite media 15.65s contra um teto de 15 constante no domínio, e a medição ruidosa (±1s) convidava a escolher a rodada boa. O comp-50 fecha as três pontas. **Como medir**: o artefato `validation` aceita `suite.runs` inline (`{ …, runs: [a, b, c] }`, ≥ 3 rodadas consecutivas como regra cruzada do contrato, não do schema); a **pior rodada** é a parede efetiva e `wall_seconds` tem que reportá-la (`≠ worst run … (report the worst)`), com a razão de orçamento emitida uma vez. **Quem define o teto**: o global do quadro (`globalSchema`/`BoardGlobal`, antes só contadores) ganha `suiteBudget { seconds, setAt, reason? }` opcional — mídia antiga carrega sem bump; o value object `SuiteBudget` (`fromGlobal`, `validate`) exige **razão para subir acima de 15** (também ao regravar) e a ignora abaixo; `ScrumBoard.suiteBudget()/setSuiteBudget()` escrevem pela mesma cadeia serializada que `nextId` (`globalChain`), e o `ValidationContract` passa a nascer com `{ budgetSeconds }` do quadro em `doneGate`/`validationContract`. Tool `scrum_suite_budget { seconds?, reason? }` (0 remove); `scrum_tree`, `/scrum` e `/scrum tree` prefixam `Suite budget: 10s (board)` — ou `20s (board — above default 15s: <razão>)` — só quando o orçamento é do quadro; `scrum_item_update` acrescenta "over budget: add a test-refactor task before done" a partir do flag `overBudget` que o Model expõe em `ValidationResult` (nunca regex sobre razões; `budget_seconds` acima do teto sozinho é erro de declaração, não suite lenta). **A suite caber**: a spec previa "um Context por arquivo" e a revisão concordou — a medição mostrou outra coisa: o Context custava pouco; cada mutação custava **17ms de `fsync`** (`writeAtomic` do `dsh-storage-json`: temp + fsync + rename + fsync do diretório), ~600 mutações ≈ 10s. Saída: pacote privado `@scrum-harness/test-support` com um **backend KV em memória** (o mesmo facet `kv`, 5 métodos) — as suites continuam sobre hub + storage-domain + ScrumService reais, só o meio é um `Map`; as suites de persistência/close-reopen seguem no JSON, porque durabilidade é o que provam. Um Context por arquivo, um quadro por teste (`ctx.scrum.board(join(prefix, 'ws-N'))`, ids recomeçam em rel-1), helpers `run(name, args, { cwd })`/`act(body)` roteando para o quadro do teste e `{ cwd: null }` só no teste que **prova** o roteamento global. Resultado: **14.15s → 1.21s** (pior de 3; arquivo mais lento 0.54s), 112 → 132 testes; pela regra de folga da spec (pior ≤ 8.5s) o orçamento do quadro foi gravado em **10s**. Três rodadas de revisão adversarial (0 high em todas; 22 findings absorvidos em v2/v3), desenho revisto em construção sem recuar de fase (v2 registra o achado), e o comp-47 fecha em seguida com a validação refeita — o primeiro `done` pelo gate.

**Task kind + fase tdd** (v0.16, comp-45): desde a spr-11 as tasks nasciam em pares `[test] …` → `[code] …`, mas isso era convenção de título — 11 `[test]`, 9 `[code]` e 74 sem prefixo no quadro, e o gate `tdd → construction` exigia só "≥ 1 task". Agora `kind: test | code | other` é **dado do domínio**: `taskSchema` ganha o campo com **migração no parse** (`.transform`, sem bump — a terceira, depois de `phase` e `releaseIds`): um registro sem `kind` passa o título por `splitKindPrefix` (módulo novo `kind.ts`), o prefixo vira `kind` e **sai do título**; a View o devolve (`kindPrefix` em `format.ts`, usado por árvore, sprint, shelf e pelo `taskEntry` do snapshot do context-scrum), então a árvore de um quadro antigo renderiza idêntica. A função é **total** (achado HIGH da rodada 1: `[test]` sozinho viraria título vazio, e o storage-domain só valida na leitura — a próxima escrita inviabilizaria o open do quadro inteiro): um título que é só o prefixo carrega como `other` intacto, com `prefixOnly: true`. Todo ponto de entrada de título passa por **um** lugar de precedência, `ScrumBoard.resolveTaskKind` (`narrowKind` no padrão `narrowStatus` → `splitKindPrefix` → `prefixOnly` recusado com `invalid-input` → prefixo que **contradiz** um `kind` explícito recusado com `kind-conflict`, comparado pelo prefixo casado → `requireTitle`), rodando **antes** do `nextId` (entrada inválida não consome contador); renomear para `[code] X` move o kind, título sem prefixo não mexe nele, e trocar o kind nunca toca id/`createdAt`/`order`. **Gate do tdd com dentes**: sair de `tdd` exige ≥ 1 task `test` e nenhuma `code` criada antes da primeira `test` — ordem pelo **número** do id (`taskNumber`; `createdAt` empata no mesmo ms e `order` reusa valores após purge; o teste cruza a fronteira `task-9`/`task-10`), todas as razões nomeadas com `; ` (sem test, toda code conta como anterior), `other` livre; a saída legítima para uma code que nasceu cedo é trocar seu kind. Tools (`scrum_task_create`/`scrum_item_update { kind }`, enum), API (`z.enum` de transporte → 400; `kind-conflict`/`invalid-kind`/`invalid-input` do Model → 409 com `code`; item da shelf carrega `taskKind` — `kind` continua sendo o **nível**) e GUI (chip `test`/`code` em tokens Primer accent/success no board, na árvore e na shelf; `select` "Tipo" no work item form gravando só `{ kind }`). Fora, com razão: gate de criação, pareamento 1:1 e a task de refactor obrigatória adiada pelo comp-50 R4 — o gate de done já bloqueia por `wall > budget` e o domínio não tem como verificar que uma task "refactor" endereça o orçamento; fica a orientação textual, agora citando o kind. Três rodadas de revisão adversarial (1H/5M/6L → 0/1/1 → **approved** 0/0/2, os dois LOW absorvidos no desenho), 23 testes novos (132 → 155; pior de 3 rodadas 2.50s contra orçamento 10s). Dogfood pelo caminho honesto (achado M2): as tasks do comp-45 nasceram sob v0.15 com o prefixo no título, então após o rebuild o componente **recua** para `tdd` e **reavança** pelo gate novo, com suas tasks migradas na leitura.

**Context-scrum ciente da fase** (v0.17, comp-46): nasceu de uma pergunta do usuário — "a injeção de contexto parou de funcionar?" — logo depois de a spr-15 ser encerrada. Não tinha parado: o desenho da v0.7 injetava só com **sprint ativa**, e entre sprints (exatamente quando o agente decide o que abrir) ele ficava cego; além disso o snapshot mostrava `comp-45 [in_progress]` sem fase nem checklist, e um componente `in_progress` **sem task na sprint** (requirements, design, tdd antes das tasks) nem aparecia. O comp-46 fecha as três. **Model**: `phaseReadiness(id)` — o gate lido sem mutar: `{ phase, status, next, ok, reasons }`, com `reasons` **exatamente** o que `advancePhase` recusaria (o `phaseGate` privado passou a devolver `string[]`; `setPhase` junta com `; `, mensagens idênticas), o `doneGate` em `validation` (`next: 'done'`), e curto-circuito em `done` (`next: null`, sem avaliar gate); `tree()` anota `readiness` em todo componente (na leitura, nunca persistido), então a API leva o campo de graça — sem action nova. **View**: `snapshot.ts` vira uma função pura com dois modos sobre os mesmos helpers (`cut`, `checklist`, `componentEntry`). Com sprint: `Pais:` ganha `comp-45 [in_progress · tdd → construction: no test task (kind test) — write the tests first]` (razões cortadas a 160), a linha nova `Em andamento (sem task na sprint): …` (≤ 5, `+N`; entra depois de `Pais:` ou logo após as colunas quando `Pais:` não existe) e a frase da espiral na disciplina — "de validation para done use `scrum_item_update status done`", porque `advance` recusa na última fase. Sem sprint, **só em quadros com componente vivo** (zero ruído em workspaces sem SCRUM e no quadro global vazio): a sprint de maior número decide o cabeçalho (`última: spr-15 #15 [completed] 9 tasks · 19 pts entregues — "…"` — "entregues" porque após `endSprint` só as done guardam o `sprintId`, e `N/N` seria tautológico; ou `planejada: spr-16 … 3 tasks · 7 pts`), `Em andamento` ≤ 5 com razões a 100, `Backlog` ≤ 6 com títulos a 48, e `Próximo passo` por precedência (`inicie a sprint (scrum_sprint_start spr-16)` → `planeje a sprint …` → `crie o próximo componente … ou arquive os concluídos`). O teto do modo sem sprint é **1.800 chars**, provado por aritmética na revisão (1.634 com ids de 3 dígitos) depois que a v1 prometeu 1.500 e o revisor mostrou que as próprias strings davam 1.920 — e o teste **constrói** o pior caso (20 propostos, 10 em andamento com razões longas). Custos declarados: cada instância de agente (subagentes incluídos) paga o snapshot no 1º turno; entre sprints o texto muda a cada edição de artefato. **Controller**: `scrum_component_phase { action: 'check' }` em quatro formas (bloqueado / `ok — advance when ready` / `→ done: ok — set status done with scrum_item_update` / `[done · validation]: nothing to do`), `presentCall` `read`. Fixtures do gate de requisitos (`CONTRACT_REQ`, `contractReview`) saíram do spec do domínio para `@scrum-harness/test-support/fixtures.ts`, puras (o pacote não referencia o domínio). Três rodadas de revisão adversarial (3H/6M/7L → 1H/0M/3L → **approved** 0/0/2; o HIGH da rodada 2 foi o ponto cego "sem task na sprint", descoberto olhando o estado real do quadro — spr-16 ativa com 0 tasks), 20 testes novos (155 → 175; pior de 3 rodadas 1.92s). Dogfood em três estados observados na própria sessão: spr-16 ativa sem tasks → a linha nova com o comp-46; tasks na sprint → `Pais:` com fase; sprint encerrada → o modo sem sprint com `última: spr-16`.

**Matriz de rastreabilidade** (v0.18, comp-49): desde a spr-11 todo desenho carregava um `traces:` no frontmatter e toda validação fechava com uma "traceability matrix" em prosa — convenção, não dado: nenhum gate lia a matriz, nenhuma tool respondia "mexer neste arquivo afeta o quê", e a varredura do quadro mostrou que **todos** os cinco componentes done com `traces` tinham requisitos sem rastro (comp-42 R8; comp-45 R7; comp-46 R6/R9; comp-47 R8/R9; comp-48 R8) e entradas que não eram caminhos. O comp-49 torna a matriz **dado do domínio sem tocar o schema**: ela continua no frontmatter que já existe — o `design` é o plano, e a `validation` pode carregar o *as-built* (`traces:` com valor array, vazio incluído), que então vence. **Model** (`traces.ts`, quarto membro da família de contratos): `TraceContract` verifica a forma (uma entrada inline por linha `- { req: [R1], files: [...], tests: [...] }`, as três chaves obrigatórias, caminhos relativos ao workspace por `normalizePath`/`pathIssue`) e a cobertura **contra os ids atuais dos requisitos** — `RequirementsContract.ids` extrai `R<n>[letra]` do corpo por linha (lista, numeração, heading e negrito aceitos; o `ids:` do frontmatter é ignorado) — nomeando todas as razões de uma vez: `traces missing`, `traces is empty`, `traces[1].files[0] "…" is not a workspace-relative path (absolute)`, `traces name unknown requirement(s) R12 (ids found: …)`, `requirement(s) without trace: R6, R9 (add an entry; files: [] for a requirement without code)`; `files: []` declara requisito sem código, `tests: []` fica como `unproven` (nunca bloqueia). `TraceMatrix.of` é a leitura leniente do mesmo dado — objeto plano deep-frozen com `source`, `entries`, `ids` e os conjuntos derivados **por id agregando todas as entradas** (`untraced`, `unknown`, `nocode`, `unproven`), `files`/`tests` por code point e `issues` informativas — anotado no `tree()` em todo componente (a API leva de graça, sem action nova). **Gates com dentes**: `design → tdd` mantém `design is empty` exato e, com desenho preenchido, exige a matriz completa; o `done` lê a fonte efetiva — uma R acrescentada por bump de versão em construção barra o done até a matriz fechar (no desenho ou no as-built). `impact(path, { onDisk?, onDiskTruncated? })` é puro sobre o quadro (`match(p, q) = q === '' || p === q || p.startsWith(q + '/')`; arquivados entram, lixados não; `missing` = casado que não está no disco nem é prefixo de diretório de algo no disco; `untracedOnDisk` = o buraco de cobertura; `form` file/directory derivado no Model) — o Model nunca toca o filesystem. **Controller**: a tool `scrum_trace` com duas formas — `path` (impacto: resolve absoluto contra o cwd **lexicalmente**, sem `realpath`; sonda o disco em `probe.ts` pulando `node_modules`, `.git`, dot-names e os nomes simples do `.gitignore` da raiz reduzidos por `gitignoreNames` no Model, até 500 arquivos) e `id` (a matriz com `holes: without trace R6, R9 · unproven R4` e `issues:`); `scrum_item_update` avisa cedo pelo gate que **de fato** vai ler o campo (`traceGateFor`: `design → tdd`, `status done` ou `(validation as-built is effective)`) e marca `trace matrix stale` quando os requisitos mudam; o brief ganha `Requirement ids found: …` e a convenção de rastreabilidade viaja no bloco **House conventions**. **View**: `formatTraceMatrix`/`formatImpact` (tetos 40 hits / 60 nomes) e a seção **Rastreabilidade** read-only no work item form (tabela Req | Arquivos | Testes, chips de buraco em `--fgColor-danger`). Duas rodadas de revisão adversarial (3H/11M/11L → **approved** 0/4M/8L; os HIGH: a fórmula de casamento da raiz não casava nada, o desenho vazio duplicaria `design is empty` quebrando asserções exatas, e a regex de ids barrava `**R1** —`/`- R1 —`/`1. R1 —`; os MEDIUM da rodada 2 foram absorvidos no desenho — resolução lexical, `gitignoreNames`/`traceGateFor` no Model, `form`, e a sequência do dogfood). 52 testes novos (175 → 227; pior de 3 rodadas 1.84s), fixture compartilhada `CONTRACT_DESIGN` (a primeira matriz que os specs atravessam). Dogfood: o desenho do comp-49 carrega a **primeira matriz lida por um gate** — recuo logado `construction → design`, `check` mostrando `design → tdd: ok` sobre a matriz real, dois `advance`, e o done fechado pelo as-built da validação; `scrum_trace packages/scrum-domain/src/traces.ts` responde com o próprio comp-49 e `scrum_trace comp-46` expõe os buracos R6/R9 da história.

**A espiral no work item form** (v0.19, comp-43): a espiral existia no Model, nas tools e na API — **não na GUI**. O work item form de um componente mostrava título/estado/descrição e a tabela read-only de rastreabilidade (v0.18), mas nenhum dos quatro artefatos, nem a fase, nem o checklist, nem avançar/recuar; Backlog e Board não exibiam fase alguma. O comp-43 é **só View** (`ui-scrum`): nenhum método novo no Model, nenhuma action nova na API — tudo que o form precisa já viajava no `tree()` (`phase`, `phaseLog`, `readiness`, `readyForDone`, os quatro artefatos) e o `updateItem`/`componentPhase` já aceitavam o resto. **Trilha**: seis passos (`requirements → design → tdd → construction → validation` + o terminal `done`) com os ids **crus** do domínio — o vocabulário das tools e do snapshot, sem tradução; clicar num passo anterior recua (livre, logado), o primário **Avançar → \<next\>** avança, e em `validation` ele vira **Concluir (status done)**. A trilha é **affordance derivada do `readiness`, nunca enforcement**: quem recusa é sempre o domínio (a View só evita oferecer o que seria recusado; `status: done` com `phase ≠ validation` — componentes fechados à mão antes da v0.14 — lê como cinco fases `past` + `done` corrente). **Checklist**: o veredito do Model impresso sem reinterpretação (`Pronto para design` / `Para design:` + as razões na ordem em que o gate as nomeia) — a mesma fonte do `scrum_component_phase check` e do snapshot do context-scrum, então salvar um artefato **é** o feedback de contrato da fase. **Artefatos**: quatro blocos `<details>` com textarea monoespaçado e placeholder ensinando o frontmatter mínimo, abertos por default os que o gate corrente lê; rascunhos locais com a regra **limpo segue o servidor, sujo mantém o rascunho** (o agente edita pela tool com o form aberto e o campo intocado acompanha, o editado guarda o `•`), `''` apaga o artefato, e uma guarda de descarte (Esc/backdrop/Fechar → `confirm`, ignorando composição IME). **A recusa dentro do form**: o banner do painel fica atrás do overlay e o poll de 4s o apaga — então `run` passou a devolver um `RunOutcome` (`{ ok }` ou `{ ok: false, message }`, **nunca rejeita**) e toda mutação disparada do form (Salvar, selects, passo, primário) passa por um `runInForm` que liga `pending` (o `busy` global não alcança o modal: dois cliques em Avançar poderiam pular duas fases) e mostra a mensagem **literal** do gate num aviso local que o poll não apaga. A ordenação virou módulo próprio e testado (`settle.ts`): ticket monotônico atribuído **no disparo**, marca d'água `applied` sobre mutações, e todo resultado mais antigo — poll **ou** mutação — é descartado (a mutação descartada ainda responde ao chamador); `busy` virou contador de chamadas em voo. Fora do form, `PhaseChip` leva a fase para a linha do Backlog e o card do board de Componentes, com `ready for done` em tom de sucesso. A `ui-scrum` ganhou **sua primeira suite** (42 casos: `form.spec.ts` + `settle.spec.ts`, em Node sem jsdom — a lógica pura mora fora do React justamente para ser testável), e a rodada 1 da revisão adversarial pegou o HIGH que definiu o desenho: a trilha age sobre o **servidor**, então com rascunho sujo ela fica desabilitada com a razão dita ("o gate lê o que está no servidor") em vez de mentir sobre o que o textarea mostra.

**Render mermaid no work item form** (v0.20, comp-44): a casa escreve diagramas em todo desenho desde a spr-11 — na abertura da spr-19, 8 designs continham **19 blocos ```mermaid** (classDiagram ×7, sequenceDiagram ×6, flowchart ×4, stateDiagram-v2 ×2, de 6 a 59 linhas) — e **nunca os leu como diagrama**, só como texto num `<textarea>`. O comp-44 é **só View + build**: nada no Model, nada na API, nenhum pacote novo. Um artefato com cercas mermaid ganha, **entre o sumário e o `textarea`**, a faixa `Diagramas · N` (com `rascunho` quando o texto difere do servidor — o preview segue o **rascunho**, o oposto deliberado da máscara da trilha: o que age sobre o servidor recusa com rascunho sujo, o que só informa mostra o rascunho, rotulado) e, ligada, os N diagramas numerados com a primeira linha como legenda. O `textarea` **nunca sai da tela**: editar o frontmatter `traces:` é o que faz o gate `design → tdd` passar. **Só cercas ```mermaid são interpretadas** (subconjunto declarado: início de linha, três crases, sem `~~~`, sem atributos) — não é renderizador de markdown — e **o artefato nunca é reescrito pelo caminho do render**: o digest do `ReviewContract` e a matriz `traces:` são byte-sensíveis. Falha honesta em três formas: erro de sintaxe mostra a mensagem do motor **literal** com os irmãos intactos; `initialize` falhando vira `Diagramas indisponíveis` com "tentar de novo"; nada derruba o form. A lógica pura mora em `mermaid.ts` (gramática, semeadura, máquina de estados por bloco com watermark **por bloco** — blocos irmãos nunca se descartam — e "mesmo texto não repinta", para o poll de 4s não girar) e o adaptador em `mermaid-engine.ts` (`initialize` **adiado** ao primeiro render e refeito na troca de tema; `finally` removendo o `<div id="d<id>">` que o mermaid deixa em `document.body` **inclusive quando lança** — medido no spike: 2 renders ⇒ 21 nós órfãos). O adaptador **não importa a lib**: o import estático vive só em `index.ts`, na linha seguinte a ele `mermaid.startOnLoad = false` — porque o mermaid registra um listener de `load` que chama `run()` (varredura de `.mermaid` no documento inteiro **reescrevendo innerHTML**) e o bundle do plugin executa **antes** do `load` (scripts `async` o atrasam). **Custo, medido e declarado**: não existe lazy-load de bundle de plugin nesta plataforma (`boot.ts` cria toda linha do grafo; `assertEntriesActive` derruba o boot se qualquer uma falhar) e o loader aceita **um** arquivo CJS (o esbuild inlina os 36 `import()` do mermaid), então o motor está **dentro** de `lib/client.js` — decisão de produto entre quatro opções medidas, depois de um pacote separado ter sido especificado e derrubado em três rodadas de revisão (comprava só download paralelo, irrelevante em localhost, ao custo de uma nova causa de falha total de boot). `build.mjs` passou a `minify: true`: **3.798.331 bytes / 1.010.662 gz** (`gzip -9`; sem minify seriam 7,93 MB / 1,44 MB gz; no bundle pré-mermaid o minify economizava só 14%, o grosso é o CSS do Primer como string), a factory do plugin leva **~75 ms** para materializar no boot (74–80 ms em três cargas, contra ≤ 15 ms de qualquer outra) e o primeiro render (`initialize` adiado + 3 diagramas) **~240 ms**; o `.map` tem 13 MB e é servido pela rota dedicada do host em dev (só baixado com o DevTools aberto). O SVG mostra na **largura natural** (`svgNaturalWidth` lê o `viewBox`: o `width="100%"` que o mermaid emite encolhia um `classDiagram` de 2 100 px a 35%, ilegível — achado do dogfood) dentro de um contêiner que rola nos dois eixos. A `ui-scrum` foi de 42 para **94 casos** (`mermaid.spec.ts` prova a gramática contra um **corpus congelado** dos 23 blocos do quadro — snapshot com data e inventário, gerado por `scripts/freeze-corpus.mjs`, não espelho; `mermaid-engine.spec.ts` prova o **contrato do adaptador** com um duplo de documento, e diz explicitamente que não prova nada sobre o mermaid real — isso é observado no browser na validação, com três contagens de vazamento escopadas). Os requisitos levaram **seis versões e seis rodadas adversariais** (~60 achados): as rodadas 2–4 gastaram todos os HIGH provando, linha a linha no host, que três mecanismos de carga sucessivos não existiam nesta plataforma — e o **carimbo humano `status: approved` foi dado pelo próprio form da v0.19**, fechando o action-item da retro anterior.

**Título é título** (v0.21, comp-53): medido no quadro na abertura da spr-20, as tasks tinham p50 75 e máx 532 chars de título (58 acima de 80, 126 sem descrição) e 18 de 19 metas de sprint passavam de 80 — o vício era do agente, e o snapshot do context-scrum o escondia cortando **na leitura**. A regra virou dado do domínio: `TitleContract` (classe irmã de `SuiteBudget`, sem estado) com **80 code points** para release/feature/componente/task e **120** para a meta de sprint (o `GOAL_CHARS` que o snapshot já imprime inteiro), sem quebra de linha nem caractere de controle; o gate morde nas **sete** escritas de título/meta — e em nenhuma outra — com `title-contract` e razão literal (`comp-53: title too long: 360 > 80 chars — move the detail to description`), avaliado **antes** de `nextId` (três `create*` e `planSprint` foram reordenados: recusa não queima id nem número de sprint). **Legado lido, marcado, nunca rejeitado**: o parse não muda; `tree()`/`sprints()`/`activeSprint()`/`sprintStatus()` anotam `titleOverflow`/`goalOverflow` só em leitura (nunca na mídia), um item legado continua operável em tudo que não seja reescrever o título, e o `scrum_tree` ganha ` [meta longa 850/120]` na linha e um cabeçalho `Title limit: 80 chars (sprint goal 120) — N title(s) and M goal(s) over` só quando há overflow. A GUI lê o limite de `state.limits` (nunca carrega um número): contador `N/80` no form, no QuickAdd e no PlanForm, chips `título longo`/`meta longa`, e — achado da revisão adversarial — a criação inline **preserva o rascunho na recusa** (antes `QuickAdd`/`PlanForm` limpavam antes de saber a resposta). Dois turnos de dogfood: a mesma `scrum_task_create` de 360 chars **passou** antes do restart do DSH e foi **recusada** depois; a meta da própria spr-20 (193/120), escrita antes do gate, ficou marcada — o vício, na sprint que o corrige. A descrição dos parâmetros das tools e o aviso no snapshot são do comp-54.

**O QUÊ e o COMO** (v0.21, comp-54): o comp-53 pôs o gate; sobravam a **causa** e a **leitura**. A causa: `scrum_task_create`/`component_create`/`feature_create` declaravam `title` e `description` **sem descrição de parâmetro** — o agente lê isso antes de escrever, e enfiava tudo no obrigatório. Agora as 5 `title`/`name`, 2 `goal` e 5 `description` dizem o contrato (`The WHAT, one line that fits a card: ≤ 80 chars, no line break. Details go to description.` / `The HOW: requirements covered (Rn), cases, numbers, deviations — this is where the paragraph goes.`), com o número montado de `TitleContract.limits()` no registro — o Controller nunca digita 80. A leitura: o snapshot do context-scrum cortava task em 64 e componente em 48 por números próprios, escondendo o problema; passa a cortar nos limites do Model nos dois modos (um título válido nunca é truncado; legado ganha `…`) e ganha a linha **`Higiene:`** — só quando há algo a dizer: tasks **abertas** da sprint sem descrição, títulos na faixa de aviso (`TitleContract.tone`, 80% do limite, o mesmo amarelo da GUI), legado acima do limite (`sprintStatus().tasks` passa a viajar marcado) — capada em 3 ids por parte, antes da Disciplina, vista no step 1 do turno seguinte e sumindo ao corrigir (a chave de reinjeção é o texto). Dogfood em três turnos reais: uma task criada **sem descrição de propósito** fez o snapshot do turno seguinte ao restart chegar com `Higiene: 1 task(s) da sprint sem descrição (task-152) — o título é o QUÊ; o COMO vai na descrição.`; corrigida pela tool, a linha sumiu. O brief de revisão adversarial carrega a regra nas convenções da casa. Pior caso do snapshot ativo medido: 3 855 chars (30 tasks de 80 sem descrição) — sentinela, não orçamento; o idle segue ≤ 1 800.

## Pacotes

| Pacote | Papel | ctx key |
|---|---|---|
| `packages/scrum-domain` | Estado durável (via `ctx.storageDomain`, backend JSON, um meio por workspace) + regras de negócio no `ScrumBoard`; `ctx.scrum.board(cwd)` resolve o quadro | `ctx.scrum` |
| `packages/tool-scrum` | 27 tools para o modelo (`scrum_tree`, `scrum_sprint_plan`, `scrum_task_move`, `scrum_component_phase`, `scrum_component_review_brief`, `scrum_trace`, `scrum_suite_budget`, `scrum_trash_list`, `scrum_item_archive`…) | registra em `ctx.tools` |
| `packages/command-scrum` | Comando humano `/scrum` (leitura: tree, sprints, status, ceremonies, trash, archive) | registra em `ctx.commands` |
| `packages/context-scrum` | Contexto de agente ciente do quadro: snapshot da sprint ativa injetado no 1º step do turno quando muda | listener `agent/pre-step` |
| `packages/scrum-api` | Rotas HTTP `/scrum-api/state` e `/scrum-api/action` para o quadro web | registra em `ctx.webServer` |
| `packages/ui-scrum` | Quadro visual na GUI web: aba **▦ SCRUM** no anel de views da conversa (Backlog em grade estilo Azure DevOps, work item form modal, Boards multinível com drag-and-drop, Sprints, Arquivo, Lixeira) | slot `conversation.view` |
| `packages/bundle-scrum` | Bundle instalável (`dsh.bundle`) que insere as linhas acima sobre o perfil web | — |
| `packages/test-support` | Só testes (privado): backend KV `memory` para as suites rodarem sobre a stack real sem `fsync` por mutação | registra em `ctx.storage` |

Regras de negócio centralizadas no `ScrumService`: pais precisam existir (e estar vivos); no máximo **uma sprint ativa**; encerrar sprint devolve tarefas não concluídas ao backlog; movimentos no board só na sprint ativa; cerimônias são append-only; exclusão de subárvore exige `cascade` e vai para a **lixeira** (restaurar com `restoreItem`, apagar de vez com `purgeItem`/`emptyTrash`); concluídos podem ir ao **arquivo** (`archiveItem`/`unarchiveItem`/`archiveCompleted`); sprints e cerimônias são história — nunca vão à lixeira, e uma release purgada apenas desvincula suas sprints.

## Rodando

Pré-requisito: o checkout do DSH ao lado (`../deepseek-harness`) com `lib/` buildado (as dependências são `file:` links para ele).

```sh
npm install --no-bin-links   # instala e linka as dependências
npx tsc --build              # compila os pacotes
(cd packages/ui-scrum && node build.mjs)  # bundle do browser
npx vitest run               # 361 testes (storage/tools/HTTP reais, backend em memória; ui-scrum em Node sem jsdom)

bash scripts/setup-profile.sh  # cria o DSH_HOME hermético (.dsh-home) e o perfil "scrum"
bash scripts/serve.sh 3090     # sobe a GUI web com o perfil scrum
```

Abra `http://127.0.0.1:3090` — a aba **▦ SCRUM** fica no topo da conversa, ao lado de `Chat` e `Trajectory`. Os dados vivem em `.dsh-home/storages/scrum_ws_<hash>.json` (um quadro por workspace; compartilhados entre a GUI, as tools do modelo e o `/scrum`).

O perfil `scrum` só existe neste repositório (`DSH_HOME` próprio em `.dsh-home/`): o harness aberto de qualquer outra pasta/perfil não vê nada disso.

**Login compartilhado**: o `setup-profile.sh` gera `.dsh-home/profiles/scrum/cordis.patch.yml` apontando as rows `settings` e `credentials` para os arquivos do seu `~/.dsh` (ambos os plugins aceitam `path` e observam o arquivo). Assim o sign-in na Anthropic, as chaves de API e o modelo padrão do harness normal valem também aqui — enquanto sessões, workspaces e os dados SCRUM continuam isolados no `.dsh-home`. Outra home real: `REAL_DSH_HOME=/caminho bash scripts/setup-profile.sh`.

## Arquitetura

- **Convenção (rel-17 em diante): MVC e orientação a objetos.** *Model* = `scrum-domain` (as classes `ScrumService`/`ScrumBoard` concentram regras e invariantes — nenhuma regra vive fora dele); *Controller* = `tool-scrum`, `scrum-api`, `command-scrum` (traduzem chamada → método do modelo, sem regra própria); *View* = `format.ts` (texto) e `ui-scrum` (React). O desenho de todo componente declara sua divisão M/V/C e as classes envolvidas; a revisão adversarial confere.
- **Persistência**: `defineDomain` do `@deepseek-ai/dsh-storage-domain` (tabelas zod-validadas: releases, features, components, tasks, sprints, ceremonies + global com contadores de id e o orçamento da suite). Dados sobrevivem às sessões e são compartilhados entre modelo, comando e GUI.
- **Tools**: padrão `defineTool` do `@deepseek-ai/dsh-tools`, saída textual id-first que o modelo referencia de volta (`rel-1`, `task-42`…).
- **UI**: plugin client (`dsh.client`) no formato de factory do module loader do DSH; registra a aba no anel `conversation.view` via `ctx.slots.inject`; estado de visualização em um store `defineStore` de escopo de sessão (uma instância por sessão); dados via `fetch` same-origin nas rotas do `scrum-api` (com polling enquanto a aba está montada).
- **Instalação**: `packages/bundle-scrum` declara `"dsh": {"bundle": {"patch": "./cordis.patch.yml"}}` — o mecanismo oficial de patch-layer de perfis do DSH (`dsh plugin --profile <nome> add <pacote>` num DSH instalado; aqui, o perfil é montado por `scripts/setup-profile.sh` com symlinks).

## Postmortem: "Failed to load plugins" na aba ▦ SCRUM (01/09)

Ao registrar a aba **▦ SCRUM** no ring de views da conversa (`conversation.view`, junto de Chat e Trajectory), a GUI caiu inteira com `Failed to load plugins — @scrum-harness/ui: store handle mounted under "sidebar.footer.action" (scope "root") is already mounted under scope "session"`. Causa raiz: o `apply` passava o **mesmo** handle de `createScrumStore()` para o botão/painel (slots `sidebar.footer.action` + `shell.overlay`, escopo `root`) e para a aba nova (escopo `session`) — e o slot system do DSH exige **one handle, one scope**: o registro seguinte ao primeiro mount lança, e o loader derruba o plugin inteiro. Conserto: um `viewStore = createScrumStore()` dedicado ao registro da aba (instância por sessão). Compartilhar um handle entre slots continua certo **dentro do mesmo escopo** (botão ↔ painel); entre escopos, compartilhe por dados — ambos os lados consultam o mesmo `/scrum-api`, que é o que o polling já faz.

Armadilha que mascarou o diagnóstico: um `node` antigo segurando a porta 3090 — `serve.sh` novos morriam em `EADDRINUSE`, e o processo velho rodava os halves de host de antes do rebuild enquanto entregava o `client.js` novo do disco. Depois de rebuildar, derrube o servidor antigo antes de subir outro (`lsof -nP -iTCP:3090 -sTCP:LISTEN` mostra quem segura a porta).
